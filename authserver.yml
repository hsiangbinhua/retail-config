server:
  port: 9999
  contextPath: /uaa 

security:
  user:
    password: password

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: ${ocp.services.eureka.credentials.uri:http://localhost:8761}/eureka/
