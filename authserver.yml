server:
  port: 9999
  contextPath: /uaa 

eureka:
  client:
    serviceUrl:
      defaultZone: ${ocp.services.eureka.credentials.uri:http://localhost:8761}/eureka/

security:
  user:
    password: password


---
eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
