info:
  app:
    name: Retail OCP Orders service
    description: Retail OCP Orders Microservice

server:
  port: 8083

eureka:
  instance:
    preferIpAddress: true
    hostname: ${ocp.application.uris[0]}
    nonSecurePort: 10080
  client:
    serviceUrl:
      defaultZone: http://eureka-primary:8761/eureka/,http://eureka-secondary:8761/eureka/,http://eureka-tertiary:8761/eureka/

#spring:
#  zipkin:
#    baseUrl: ${ocp.services.eureka.credentials.uri:http://localhost:9411}
